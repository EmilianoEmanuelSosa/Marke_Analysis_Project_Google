upstream django {
    server django_gunicorn:8000;
}

server {
    listen 80;
    location / {
        proxy_pass http://django;
    }
    location /static/ {
        alias /staticfiles/;  # Point to the directory where static files are collected
    }
}